# Code Review: 805d9a1

**Commit:** 805d9a1  
**Date:** 2026-06-23  
**Author:** Johannes Bechberger  
**Message:** style(v2): extract inline DepGraph empty fallback to named variable

## Result: PASS

## Summary

Single-file refactor of `DepGraphSource.tsx`: the inline `{ nodes: [], edges: [] }` fallback object was
extracted to a named `emptyGraph` constant and `cycles: []` was added to match the full `DepGraph` shape.

## Lint Errors

None. ESLint exits 0 with `--max-warnings 0`.

## Type Errors

None. `npx tsc --noEmit` exits 0.

## Formatting Issues

None. All matched files use Prettier code style.

## Auto-fixes Applied

None required.

## Notes

- The added `cycles: []` field in the fallback is a correctness improvement; previous code omitted the
  field which could cause downstream consumers to crash if they accessed `graph.cycles` on the fallback.
- Change is minimal and safe.
